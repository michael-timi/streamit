# Default local config (safe to commit).
# Set to your API origin, e.g. http://10.0.2.2:3000 (Android emulator) or http://127.0.0.1:3000
# Leave empty to run without a backend: /auth/me is not called and the app starts as signed out.
API_BASE_URL=
